package JULY.ex_27072024_Arrays;

import java.lang.reflect.Array;
import java.util.Arrays;
import java.util.Collection;
import java.util.Collections;

public class Lab033 {
    public static void main(String[] args) {
        //Interview Question
        //Find the maximum in Array or Find the max salary in array

//        int[] salary = {7, 25, 14, 10, 55, 5, 9, 34};
//        int max = 1;
//        for (int i = 0; i <salary.length; i++) {
//            if (salary[i] > max) {
//                max = salary[i];
//            }
//        }
//        System.out.println(max);
//
//        //or
//
//        Arrays.sort(salary);
//        System.out.println(salary[salary.length-1]);


        //Find the minimum in Array or Find the max salary in array
//        int[] salary = {7, 25, 14, 10, 55, 5, 9, 34};
//        int min = salary[0];
//        for (int i = 0; i <salary.length; i++) {
//            if (min > salary[i]){
//                min = salary[i];
//            }
//        }
//        System.out.println(min);

//        Arrays.sort(salary, Collections.reverseOrder());
//        System.out.println(Arrays.toString(salary));


        //Write program to assend the given numbers
//        int[] salary = {7, 25, 14, 10, 55, 5, 9, 34};
//        System.out.println(Arrays.toString(salary));
//        Arrays.sort(salary);
//        System.out.println(Arrays.toString(salary));


        //In desending order
        // Initialize an array of integers
//        Integer[] salaries = {5, 2, 8, 3, 1, 9};

        // Print the original array
//        System.out.println("Original array: " + Arrays.toString(numbers));

        // Sort the array in descending order
//        Arrays.sort(salaries, Collections.reverseOrder());

        // Print the sorted array
//        System.out.println("Sorted array (descending): " + Arrays.toString(salaries));


//        String[] family = {"Prakash", "Shantha", "Praveen"};
//        Arrays.sort(family);
//        System.out.println(Arrays.toString(family));
//
//        Arrays.sort(family, Collections.reverseOrder());
//        System.out.println(Arrays.toString(family));


    }
}
